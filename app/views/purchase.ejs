<!DOCTYPE html>
<html>
<head>
  <title>Payment Details</title>
  <style>
    /* Add your CSS styles here */
    body {
      font-family: Arial, sans-serif;
      background-color: #f2f2f2;
    }

    h1 {
      color: #333333;
      text-align: center;
      margin-top: 50px;
    }

    .payment-info {
      background-color: #ffffff;
      border: 1px solid #dddddd;
      padding: 20px;
      margin: 20px auto;
      max-width: 400px;
      box-shadow: 0px 0px 5px rgba(0, 0, 0, 0.1);
    }

    .payment-info p {
      margin-bottom: 10px;
    }
  </style>
</head>
<body>
  <h1>Payment Details</h1>
  <div class="payment-info">
    <p>Order ID: {{ paymentData.order_id }}</p>
    <p>Name: {{ paymentData.name }}</p>
    <p>Amount: {{ paymentData.amount }} {{ paymentData.currency }}</p>
    <p>Payment Status: {{ paymentData.status }}</p>
    <p>Payment Date: {{ paymentData.date }}</p>
    <p>Transaction ID: {{ paymentData.transaction_id }}</p>
    <p>Payment Method: {{ paymentData.payment_by }}</p>
    <!-- <p>Payment Phone: {{ paymentData.payment_by }}</p> -->
    <p>Payment Description: {{ paymentData.description }}</p>
    <p>Email: {{ paymentData.email }}</p>
    <p>Contact: {{ paymentData.payment_by }}</p>
    <p>RRN: {{ paymentData.rrn }}</p>
    <p>UPI Transaction ID: {{ paymentData.upi_transaction_id }}</p>
    <p class="{{ paymentStatus === 'success' ? 'success' : 'failure' }}">{{ paymentStatus }}</p>
  </div>
</body>
  </div>
  <script>
    function getPaymentStatus() {
  // You can modify this function to determine the payment status based on your specific requirements
  // Here, 'created' is considered as success, and other values are considered as failure
  if (status === 'created') {
    return 'Success';
  } else {
    return 'Failure';
  }
}

  </script>
</body>
</html>
